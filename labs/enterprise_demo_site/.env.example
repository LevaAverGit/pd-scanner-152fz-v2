# Lab admin panel password — set to any value for local use
LAB_ADMIN_PASSWORD=change-me

# Profile mode: good_compliance | mixed_compliance | bad_compliance
# Set via API: POST /api/profile/{profile_name}
# Or write directly to backend/data/profile.json
